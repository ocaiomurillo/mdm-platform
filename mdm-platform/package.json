{
  "name": "mdm-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:up": "docker compose -f infra/docker/docker-compose.dev.yml up -d db pgadmin",
    "dev:up": "docker compose -f infra/docker/docker-compose.dev.yml up -d"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "vitest": "^2.1.4"
  }
}
